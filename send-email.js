const sentEmail = (recipient, sender) => {
  console.log(`Email has been sent to ${recipient} from ${sender}`);
};

const sentVerification = (recipient, token) => {
  console.log(`Token : ${token}`);
};
